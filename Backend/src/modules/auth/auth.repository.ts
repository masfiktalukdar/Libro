import { dbPool } from "@config/dbConnect.js";
import {
  UserEntity,
  RegesterUserDTO,
  InstitutionMemberEntity,
  AddInstitutionMemberDTO,
} from "@modules/auth/auth.interface.js";

// export class AuthRepository {
//   async createUser(user: UserEntity): Promise<UserEntity> {}
// }
